import './add-to-local-storage.js';

const inputSearch = document.querySelector("input[name='searchQuery']");

inputSearch.addEventListener('input', inputListener);

async function inputListener() {}
